//
// Created by marcfsr on 2019-01-29.
//

#include "action.h"

// todo : implement bellow lines

/*
using namespace ipolitic;

const int TH_PL_CNT_GRP = 5;

void insert(action act, char grp) {
    bool isEmpty = false;
    for (int i = 0; i < TH_PL_CNT_GRP; i += 1) {
        if (!false) // if thread grp grp at i is empty
            isEmpty = true;
    }

    if (!isEmpty) {
        if ((int) grp) < ((int) 'D')) {
            // insert act into thread
        }

        if (((int) grp) === ((int) 'D')) {
            // insert act into thread
        }
    }
}

 */